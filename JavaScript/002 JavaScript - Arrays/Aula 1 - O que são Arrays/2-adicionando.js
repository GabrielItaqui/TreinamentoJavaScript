const notas = [10, 6, 8]
console.log(notas)

//para adicionar elementos ao array usando o comando .push
notas.push(7);
console.log(notas) 
//como podemos ver no console, a nota foi adicionada.

const media = (notas[0] + notas[1] + notas[2] + notas[3]) / notas.length

console.log(media)
