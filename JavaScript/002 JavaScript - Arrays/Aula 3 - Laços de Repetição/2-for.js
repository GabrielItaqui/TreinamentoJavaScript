const numeros = [100, 200, 300, 400, 500, 600];

//Primeira expressão é executada apenas uma unica vez e define qual é a variávei e qual o valor inicial dela.

//Segunda expressão é a condição de execução.

//Terceira expressão é a variavel de alteração, e é executada sempre ao final do bloco.

for (let i = 0; i < numeros.length; i++) {
    console.log(numeros[i]);
}